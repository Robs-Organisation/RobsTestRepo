name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15

    - name: Build
      run: go build -v ./...

    - name: download Buffalo
      run: wget https://github.com/gobuffalo/buffalo/archive/v0.16.20.tar.gz
      
    - name: unzip download
      run: tar -xvzf v0.16.20.tar.gz
    
    - name: check dir
      run: ls
    
    - name: check if installed
      run: buffalo
